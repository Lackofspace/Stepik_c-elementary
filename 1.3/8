#include <iostream>

int main() {
    int n = 0;
    std::cin >> n;
    int two_digit_number = n % 100;
    std::cout << (n - two_digit_number) / 100 + (two_digit_number / 10) + (two_digit_number % 10);
    return 0;
}
